import java.util.Scanner;

/**
 * Name:
 * Date Due:
 * Description:
 */
public class FractionCalculator {
    public static void main(String[] args) {
        //your code here
    }

    /**
     * Gets an operation from the user as a String.
     * Valid operations are "+", "-", "/", "*", "=", "q" or "Q".
     * @param input a Scanner to get keyboard input.
     * @return a valid operation.
     */
    public static String getOperation(Scanner input) {
        //your code here
        return null;
    }

    /**
     * Gets a Fraction from the user.
     * @param input a Scanner to get keyboard input.
     * @return a valid Fraction.
     */
    public static Fraction getFraction(Scanner input) {
        //your code here
        return null;
    }

    /**
     * Checks a String to be a valid Fraction format.
     * Valid Strings are either "a/b" or "a", where a is an integer (positive, negatiive or zero)
     * and b is a positive integer.
     * @param str a String to check.
     * @return true if the String is valid,
     *         false otherwise.
     */
    public static boolean validFraction(String str) {
        //your code here
        return false;
    }
}
